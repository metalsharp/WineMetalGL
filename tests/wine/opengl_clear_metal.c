#include <windows.h>
#include <GL/gl.h>
#include <stdio.h>
int main(void){PIXELFORMATDESCRIPTOR p={0};HWND w;HDC dc;HGLRC c;int f;unsigned char px[4]={0};w=CreateWindowA("STATIC","WineMetalGL clear",WS_OVERLAPPEDWINDOW,0,0,64,64,0,0,0,0);if(!w)return 11;dc=GetDC(w);p.nSize=sizeof(p);p.nVersion=1;p.dwFlags=PFD_DRAW_TO_WINDOW|PFD_SUPPORT_OPENGL;p.iPixelType=PFD_TYPE_RGBA;p.cColorBits=32;f=ChoosePixelFormat(dc,&p);if(!f||!SetPixelFormat(dc,f,&p))return 12;c=wglCreateContext(dc);if(!c||!wglMakeCurrent(dc,c))return 13;glViewport(0,0,8,8);glClearColor(.2f,.4f,.6f,1.f);glClear(GL_COLOR_BUFFER_BIT);glReadPixels(4,4,1,1,GL_RGBA,GL_UNSIGNED_BYTE,px);printf("Metal clear rgba=%u,%u,%u,%u error=0x%x\n",px[0],px[1],px[2],px[3],(unsigned)glGetError());if(px[0]<47||px[0]>55||px[1]<98||px[1]>106||px[2]<149||px[2]>157||px[3]<250)return 14;printf("WINEMETALGL_CLEAR_OK\n"); const float clear_value[4]={.6f,.2f,.1f,1.f}; void (WINAPI *clear_buffer)(GLenum,GLint,const GLfloat*)=(void (WINAPI*)(GLenum,GLint,const GLfloat*))wglGetProcAddress("glClearBufferfv"); if(!clear_buffer)return 15; clear_buffer(0x1800,0,clear_value); glReadPixels(4,4,1,1,GL_RGBA,GL_UNSIGNED_BYTE,px); if(px[0]<149||px[0]>157||px[1]<47||px[1]>55||px[2]<20||px[2]>30||px[3]<250)return 15; printf("WINEMETALGL_CLEAR_BUFFER_OK\\n"); SwapBuffers(dc);wglMakeCurrent(0,0);wglDeleteContext(c);ReleaseDC(w,dc);DestroyWindow(w);return 0;}
